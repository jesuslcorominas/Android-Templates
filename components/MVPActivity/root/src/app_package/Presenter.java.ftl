package ${packageName}.presenter;

import ${packageName}.view.callbackview.${className}View;

/**
 * {@inheritDoc}
 *
 * Presenter de la ${packageName}.view.activity.${className}Activity
 */
public interface ${className}Presenter extends Presenter <${className}View> {

}